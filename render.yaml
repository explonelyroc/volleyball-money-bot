services:
  - type: web
    name: volleyball-money-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    pythonVersion: 3.11.9
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DB_PATH
        value: bot.db